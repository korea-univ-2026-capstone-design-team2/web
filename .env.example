# Backend API base URL used by client-side services.
# Local example: http://localhost:8080
# Vercel: set the same key in Project Settings > Environment Variables before build/deploy.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
